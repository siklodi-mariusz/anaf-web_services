# frozen_string_literal: true

RSpec.describe Anaf::WebServices do
  it "has a version number" do
    expect(Anaf::WebServices::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
